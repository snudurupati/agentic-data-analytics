.venv/
venv/
__pycache__/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Generated databases and run output
*.duckdb
*.duckdb.wal
*.db

# dbt build artifacts. Everything here is regenerated by `dbt seed` and
# `dbt run`, so none of it belongs in the repo.
target/
dbt_packages/
logs/
.user.yml

# Experiment tooling and its output. Lives in the vault, not the repo.
experiments/
**/experiments/

# Local model keys and config
.env
.env.*
!.env.example

# Claude Code permission allowlists. Machine-local, not shared.
.claude/settings.local.json

# Recording and editing artifacts, kept out of git deliberately.
# Video files belong in cloud storage, not in a repo viewers will clone.
*.mov
*.mp4
*.wav
*.aiff
recordings/
renders/

.DS_Store
.idea/
.vscode/
