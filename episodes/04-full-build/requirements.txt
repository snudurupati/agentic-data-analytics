# Python 3.12
#
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Pinned exactly. dbt and DuckDB both change behaviour between minor versions,
# so a floating install will not reproduce what is recorded.

dbt-core==1.12.0
dbt-duckdb==1.10.1
duckdb==1.5.5
